#
#
def main():
    num1 = int(input('Digite N: '))
    numero = 1
    resultado = 0
    num2 = num1
    while numero <= num1:
        resultado += numero / num2
        num2 = num2 - 1
        numero += 1
    print(f's = {resultado:.2f}')


main()
